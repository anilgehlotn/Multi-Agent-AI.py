# Google Gemini API key — used for chat + embeddings via langchain-google-genai
GOOGLE_API_KEY=your-gemini-api-key-here

# Tavily API key — used by the web_search tool in the research agents
TAVILY_API_KEY=your-tavily-api-key-here

# Mistral API key — used by the multi-query retriever demo (rag/retrievers/multiquery.py)
MISTRAL_API_KEY=your-mistral-api-key-here

# JWT + DB vars go here in the next step — leave commented placeholders
# JWT_SECRET_KEY=
# DATABASE_URL=
